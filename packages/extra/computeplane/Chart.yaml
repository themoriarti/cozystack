apiVersion: v2
name: computeplane
description: Managed, isolated environment for running code-executing apps
icon: /logos/computeplane.svg
type: application
version: 0.0.0 # Placeholder, the actual version will be automatically set during the build process
