apiVersion: v1
kind: Service
metadata:
  name: cozystack-api
  namespace: cozy-system
spec:
  {{- if .Values.cozystackAPI.localK8sAPIEndpoint.enabled }}
  internalTrafficPolicy: Local
  {{- end }}
  ports:
  - port: 443
    protocol: TCP
    targetPort: https
  selector:
    app: cozystack-api
