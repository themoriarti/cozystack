apiVersion: v2
name: kubernetes-worker-image
description: Golden Talos worker OS image collection for tenant Kubernetes clusters, pre-populated as DataVolumes in cozy-public for CDI csi-clone
type: application
version: 0.0.0 # Placeholder, the actual version will be automatically set during the build process
