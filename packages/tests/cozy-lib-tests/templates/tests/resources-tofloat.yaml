apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-tofloat
data:
{{- range $name, $quantity := .Values.quantities }}
  {{ $name }}: {{ include "cozy-lib.resources.toFloat" $quantity | quote }}
{{- end }}
