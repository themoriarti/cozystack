---
apiVersion: cozystack.io/v1alpha1
kind: PackageSource
metadata:
  name: cozystack.kubernetes-application
spec:
  sourceRef:
    kind: OCIRepository
    name: cozystack-packages
    namespace: cozy-system
    path: /
  variants:
    - name: kubevirt
      dependsOn:
        - cozystack.networking
        - cozystack.capi-operator
        - cozystack.capi-provider-bootstrap-kubeadm
        - cozystack.capi-provider-core
        - cozystack.capi-provider-cp-kamaji
        - cozystack.capi-provider-infra-kubevirt
        # cozystack-engine ships the ApplicationDefinition CRD rendered by the *-rd component below.
        - cozystack.cozystack-engine
      libraries:
        - name: cozy-lib
          path: library/cozy-lib
      components:
        - name: kubernetes-ingress-nginx
          path: system/ingress-nginx
        - name: kubernetes-cert-manager-crds
          path: system/cert-manager-crds
        - name: kubernetes-volumesnapshot-crd
          path: system/vsnap-crd
        - name: kubernetes-velero
          path: system/velero
        - name: kubernetes-gateway-api-crds
          path: system/gateway-api-crds
        - name: kubernetes-victoria-metrics-operator
          path: system/victoria-metrics-operator
        - name: kubernetes-kubevirt-csi-node
          path: system/kubevirt-csi-node
        # The storage driver for the proxmox substrate. Published alongside the
        # KubeVirt one rather than in a variant of its own: a second variant
        # would rename every tenant addon artifact, and only this component
        # differs between the two substrates.
        - name: kubernetes-proxmox-csi
          path: system/proxmox-csi
        # The cloud-controller-manager that same substrate cannot go without:
        # its worker kubelets run with --cloud-provider=external, so the nodes
        # stay tainted until this clears them, and the CSI node plugin needs
        # the topology labels it applies.
        - name: kubernetes-proxmox-ccm
          path: system/proxmox-ccm
        - name: kubernetes-vertical-pod-autoscaler-crds
          path: system/vertical-pod-autoscaler-crds
        - name: kubernetes-coredns
          path: system/coredns
        - name: kubernetes-cert-manager
          path: system/cert-manager
        - name: kubernetes-monitoring-agents
          path: system/monitoring-agents
        - name: kubernetes-cilium
          path: system/cilium
        - name: kubernetes-gpu-operator
          path: system/gpu-operator
        - name: kubernetes-hami
          path: system/hami
        - name: kubernetes-vertical-pod-autoscaler
          path: system/vertical-pod-autoscaler
        - name: kubernetes-prometheus-operator-crds
          path: system/prometheus-operator-crds
        - name: kubernetes-metrics-server
          path: system/metrics-server
        - name: kubernetes-ouroboros
          path: system/ouroboros
        - name: kubernetes
          path: apps/kubernetes
          libraries: ["cozy-lib"]
        - name: kubernetes-rd
          path: system/kubernetes-rd
          install:
            namespace: cozy-system
            releaseName: kubernetes-rd
