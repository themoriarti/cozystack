# Fixture that reproduces the default values.yaml state for tls.enabled.
# tls.enabled is intentionally omitted here so that helm-unittest does not
# inject any value and the template resolves it the way an unedited chart does:
# TLS off, regardless of .Values.external.
tls: {}
