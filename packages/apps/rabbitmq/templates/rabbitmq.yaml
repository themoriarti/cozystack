{{- $tlsEnabled := (include "rabbitmq.tls.enabled" .) | eq "true" }}
{{- $wantDisableNonTLS := dig "disableNonTLSListeners" false (.Values.tls | default dict) }}

{{- /* Closing the plaintext listeners with TLS off would leave nothing listening.
     The operator rejects this too (reconcile_tls.go), so fail here rather than
     drop the flag and render a cluster that looks healthy with every plaintext
     listener open. */}}
{{- if and $wantDisableNonTLS (not $tlsEnabled) }}
{{-   fail "tls.disableNonTLSListeners is set but TLS is off, which would leave no listener at all: set tls.enabled: true as well. The RabbitMQ operator rejects this combination too." }}
{{- end }}
{{- $disableNonTLS := and $tlsEnabled $wantDisableNonTLS }}

{{- /* The flag forces messaging-topology-operator onto TLS, where it verifies the
     per-release CA against x509.SystemCertPool() and fails, so every kind the
     operator owns stops reconciling. The .Values check understates the breakage:
     useTLSForConnection derives from the RabbitmqCluster, not from who created the
     topology CR, so hand-written objects fail the same way without this refusal.
     See README "Closing the plaintext listeners". */}}
{{- if and $disableNonTLS (or .Values.users .Values.vhosts) }}
{{-   fail "tls.disableNonTLSListeners cannot be combined with users or vhosts: closing the plaintext listeners stops messaging-topology-operator from reconciling the User, Vhost and Permission objects it declares, and from deleting them. Every other kind that operator owns breaks the same way, hand-written or not, because it reads the RabbitmqCluster and not who created the CR. Manage users and permissions with rabbitmqctl or a definitions import. Clearing them and setting the flag are two separate upgrades, in that order. See README \"Closing the plaintext listeners\"." }}
{{- end }}

{{- /* The check above reads values, so it cannot see the transition itself:
     clearing users and vhosts and setting the flag in one edit leaves both
     empty and renders cleanly. Helm applies every object in the target release
     before pruning the ones it dropped, so the broker closes plaintext and
     loses the connection annotation while the topology objects are still
     holding deletion.finalizers.<plural>.rabbitmq.com. Their operator then has
     to reach a management API it can no longer verify, so the delete never
     completes and the upgrade reports success over objects stuck in
     Terminating. Read the namespace rather than the values, and only while the
     flag is being introduced: once the live cluster already carries it the
     transition is behind us, and an object created after that is the tenant's
     to clean up rather than a reason to stop rendering. */}}
{{- if $disableNonTLS }}
{{-   $live := lookup "rabbitmq.com/v1beta1" "RabbitmqCluster" .Release.Namespace .Release.Name | default dict }}
{{-   if not (dig "spec" "tls" "disableNonTLSListeners" false $live) }}
{{-     $pending := list }}
{{-     range $kind := splitList " " (include "rabbitmq.topology.kinds" $) }}
{{-       range dig "items" (list) (lookup "rabbitmq.com/v1beta1" $kind $.Release.Namespace "" | default dict) }}
{{- /* A tenant namespace holds every RabbitMQ application the tenant runs, so
     the listing has to be narrowed to this release. A sibling's topology
     objects are reached through their own broker, whose listeners this flag
     does not touch. An object that binds through connectionSecret rather than
     by name falls outside this test and stays unguarded: the Secret it points
     at is what names the broker, and reading it here would not say which
     release wrote it. */}}
{{-         if eq (dig "spec" "rabbitmqClusterReference" "name" "" .) $.Release.Name }}
{{-           $pending = append $pending (printf "%s/%s" $kind .metadata.name) }}
{{-         end }}
{{-       end }}
{{-     end }}
{{-     if $pending }}
{{-       fail (printf "tls.disableNonTLSListeners cannot be turned on while this release still has topology objects in namespace %s: %s. Remove them in one upgrade, confirm `kubectl get %s --namespace %s` returns nothing bound to this release, then set the flag in a second upgrade. Setting both at once leaves those objects stuck in Terminating. See README \"Removing the topology objects is its own upgrade\"." $.Release.Namespace (join ", " $pending) (include "rabbitmq.topology.resources" $) $.Release.Namespace) }}
{{-     end }}
{{-   end }}
{{- end }}

apiVersion: rabbitmq.com/v1beta1
kind: RabbitmqCluster
metadata:
  name: {{ .Release.Name }}
  labels:
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  {{- if and $tlsEnabled (not $disableNonTLS) }}
  {{- /* Pins messaging-topology-operator to the plaintext management port. Without
       it, spec.tls.secretName alone flips the operator to https://…:15671, where it
       verifies the per-release CA against x509.SystemCertPool() and every User,
       Vhost and Permission fails with "certificate signed by unknown authority".
       The http:// scheme makes extractURIandScheme report tlsEnabled=false, so that
       plaintext listener is load-bearing rather than merely tolerated. Broker
       traffic is unaffected.

       Expect this key on tenant-visible objects: ReconcileAndFilterAnnotations
       strips only kubernetes.io / k8s.io keys, so the operator copies it onto every
       child it builds. See README "TLS scope". */}}
  annotations:
    rabbitmq.com/operator-connection-uri: "http://{{ .Release.Name }}.{{ .Release.Namespace }}.svc.{{ include "rabbitmq.clusterDomain" . }}:15672"
  {{- end }}
spec:
  replicas: {{ .Values.replicas }}
  image: 'rabbitmq:{{ include "rabbitmq.versionMap" $ }}-management'
  {{- if .Values.external }}
  service:
    type: LoadBalancer
  {{- end }}
  resources: {{- include "cozy-lib.resources.defaultingSanitize" (list .Values.resourcesPreset .Values.resources $) | nindent 4 }}
  {{- if $tlsEnabled }}
  {{- /* secretName is the whole of it: the operator's defaultTLSConf then supplies
       ssl_options and management.ssl itself, so no additionalConfig is needed to
       serve AMQPS or management HTTPS. That same branch writes
       management.tcp.port=15672, which is why plaintext management stays up while
       disableNonTLSListeners is unset. No cacertfile is set — it would only matter
       for verifying CLIENT certificates, which this chart does not enable. */}}
  tls:
    secretName: {{ .Release.Name }}-tls
    {{- if $disableNonTLS }}
    {{- /* Writes `listeners.tcp = none` into rabbitmq.conf: broker-level, so
         in-cluster clients lose plaintext with external ones. Does NOT cover Erlang
         distribution (25672) or epmd (4369), which ride the headless Service. See
         README "TLS scope". */}}
    disableNonTLSListeners: true
    {{- end }}
  {{- end }}
  override:
    {{- /* Deliberately no spec.override.service. It cannot remove a port — the
         override is a strategic merge keyed on `port` against defaults the operator
         regenerates each reconcile — so a TLS-only port list would be a no-op that
         reads like a guarantee. Re-adding one is worse: on a LoadBalancer it drives
         a NodePort reallocation loop through the operator's Service watch. See
         README "TLS scope". */}}
    statefulSet:
      spec:
        template:
          spec:
            enableServiceLinks: false
            containers: []
          metadata:
            labels:
              policy.cozystack.io/allow-to-apiserver: "true"

  persistence:
    {{- with .Values.storageClass }}
    storageClassName: {{ . }}
    {{- end }}
    storage: {{ .Values.size }}

{{- range $user, $u := .Values.users }}

{{- $password := $u.password }}
{{- if not $password }}
{{- with (dig "data" "password" "" (lookup "v1" "Secret" $.Release.Namespace (printf "%s-%s-credentials" $.Release.Name (kebabcase $user)))) }}
{{- $password = b64dec . }}
{{- end }}
{{- end }}
{{- if not $password }}
{{- $password = (randAlphaNum 16) }}
{{- end }}

---
apiVersion: rabbitmq.com/v1beta1
kind: User
metadata:
  name: {{ $.Release.Name }}-{{ kebabcase $user }}
  annotations:
    config: '{{ printf "%s %s" $user $password | sha256sum }}'
spec:
  importCredentialsSecret:
    name: {{ $.Release.Name }}-{{ kebabcase $user }}-credentials
  rabbitmqClusterReference:
    name: {{ $.Release.Name }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ $.Release.Name }}-{{ kebabcase $user }}-credentials
  labels:
    apps.cozystack.io/user-secret: "true"
type: Opaque
stringData:
  username: {{ $user }}
  password: {{ $password }}
{{- end }}

{{- range $host, $h := .Values.vhosts }}
---
apiVersion: rabbitmq.com/v1beta1
kind: Vhost
metadata:
  name: {{ $.Release.Name }}-{{ kebabcase $host }}
spec:
  name: {{ $host }}
  rabbitmqClusterReference:
    name: {{ $.Release.Name }}
{{- range $user := $h.roles.admin }}
---
apiVersion: rabbitmq.com/v1beta1
kind: Permission
metadata:
  name: {{ $.Release.Name }}-{{ kebabcase $host }}-{{ kebabcase $user }}
spec:
  vhost: "{{ $host }}"
  user: "{{ $user }}"
  permissions:
    write: ".*"
    configure: ".*"
    read: ".*"
  rabbitmqClusterReference:
    name: {{ $.Release.Name }}
{{- end }}
{{- range $user := $h.roles.readonly }}
---
apiVersion: rabbitmq.com/v1beta1
kind: Permission
metadata:
  name: {{ $.Release.Name }}-{{ kebabcase $host }}-{{ kebabcase $user }}
spec:
  vhost: "{{ $host }}"
  user: "{{ $user }}"
  permissions:
    read: ".*"
  rabbitmqClusterReference:
    name: {{ $.Release.Name }}
{{- end }}

{{- end }}
