{{- $clusterDomain := (index .Values._cluster "cluster-domain") | default "cozy.local" }}
{{- $tlsEnabled := (include "nats.tls.enabled" .) | eq "true" }}
{{- $existingSecret := lookup "v1" "Secret" .Release.Namespace (printf "%s-credentials" .Release.Name) }}
{{- $passwords := dict }}

{{- with (dig "data" (dict) $existingSecret) }}
  {{- range $k, $v := . }}
    {{- $_ := set $passwords $k (b64dec $v) }}
  {{- end }}
{{- end }}

{{- range $user, $u := .Values.users }}
  {{- if $u.password }}
    {{- $_ := set $passwords $user $u.password }}
  {{- else if not (index $passwords $user) }}
    {{- $_ := set $passwords $user (randAlphaNum 16) }}
  {{- end }}
{{- end }}

{{- $merge := dict }}
{{- with .Values.config.merge }}
  {{- $merge = deepCopy . }}
{{- end }}
{{- if $passwords }}
  {{- $users := list }}
  {{- range $username, $password := $passwords }}
    {{- $users = append $users (dict "user" $username "password" $password) }}
  {{- end }}
  {{- $merge = mergeOverwrite $merge (dict "accounts" (dict "A" (dict "users" $users))) }}
{{- end }}

{{- if .Values.users }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-credentials
stringData:
  {{- range $user, $u := .Values.users }}
  {{ quote $user }}: {{ quote (index $passwords $user) }}
  {{- end }}
{{- end }}

---

apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: {{ .Release.Name }}-system
  labels:
    sharding.fluxcd.io/key: tenants
spec:
  chartRef:
    kind: ExternalArtifact
    name: cozystack-nats-application-default-nats-system
    namespace: cozy-system
  interval: 5m
  timeout: 10m
  install:
    remediation:
      retries: -1
  upgrade:
    remediation:
      retries: -1
  valuesFrom:
  - kind: Secret
    name: cozystack-values
  values:
    nats:
      container:
        merge:
          resources: {{- include "cozy-lib.resources.defaultingSanitize" (list .Values.resourcesPreset .Values.resources $) | nindent 12 }}
      fullnameOverride: {{ .Release.Name }}
      {{- if $tlsEnabled }}
      tlsCA:
        enabled: true
        secretName: {{ .Release.Name }}-ca
      {{- end }}
      config:
        {{- with $merge }}
        merge:
          {{- toYaml . | nindent 10 }}
        {{- end }}
        {{- with .Values.config.resolver }}
        resolver:
          {{- toYaml . | nindent 10 }}
        {{- end }}
        {{- if $tlsEnabled }}
        nats:
          tls:
            enabled: true
            secretName: {{ .Release.Name }}-tls
        {{- end }}
        cluster:
          enabled: true
          routeURLs:
            k8sClusterDomain: {{ $clusterDomain }}
            {{- if $tlsEnabled }}
            useFQDN: true
            {{- end }}
          replicas: {{ .Values.replicas }}
          {{- if $tlsEnabled }}
          tls:
            enabled: true
            secretName: {{ .Release.Name }}-tls
          {{- end }}
        monitor:
          enabled: true
        jetstream:
          enabled: true
          fileStore:
            enabled: {{ .Values.jetstream.enabled }}
            pvc:
              enabled: true
              size: {{ .Values.jetstream.size }}
              {{- with .Values.storageClass }}
              storageClassName: {{ . }}
              {{- end }}
      {{- if .Values.external }}
      service:
        merge:
          spec:
            type: LoadBalancer
      {{- end }}
