{{- if .Values._namespace.etcd }}
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: {{ .Release.Name }}-csi
  labels:
    cozystack.io/repository: system
    cozystack.io/target-cluster-name: {{ .Release.Name }}
    sharding.fluxcd.io/key: tenants
spec:
  interval: 5m
  timeout: 10m
  releaseName: csi
  chartRef:
    kind: ExternalArtifact
    name: cozystack-kubernetes-application-kubevirt-kubernetes-kubevirt-csi-node
    namespace: cozy-system
  kubeConfig:
    secretRef:
      name: {{ .Release.Name }}-admin-kubeconfig
      key: super-admin.svc
  targetNamespace: cozy-csi
  storageNamespace: cozy-csi
  {{- /* Flux defaults to the RemediateOnFailure strategy, and install
         remediation is always an uninstall, so an install that outruns the
         spec.timeout above is torn down between attempts: the tenant loses
         its CSI node plugin and the StorageClasses this release propagates,
         and the next attempt restarts the DaemonSet rollout instead of
         resuming one already under way. RetryOnFailure keeps the manifests
         in place and retries the failed release. Both actions carry it
         because the controller picks the active retry by
         .status.lastAttemptedReleaseAction and performs that retry as an
         upgrade, so on install alone the retry's own failure falls through
         to upgrade remediation, a rollback with no previous release to
         reach. The cost is that a real upgrade failure retries rather than
         rolls back, which gives up no stable safety net: with retries: -1
         and the default rollback strategy, upgrade remediation already
         rolled back and retried without end. The remediation blocks stay
         for a different reason, that an absent release
         consults install and an out-of-sync one consults upgrade, both by
         retry count rather than by strategy, so retries: -1 keeps them
         open. */}}
  install:
    createNamespace: true
    strategy:
      name: RetryOnFailure
      retryInterval: 30s
    remediation:
      retries: -1
  upgrade:
    strategy:
      name: RetryOnFailure
      retryInterval: 30s
    remediation:
      retries: -1
  values:
    storageClasses:
    {{- /* Propagate remote-accessible LINSTOR infra StorageClasses to the tenant under the same name. */}}
    {{- $defaultName := .Values.storageClass | default "replicated" }}
    {{- $out := dict }}
    {{- range $sc := (lookup "storage.k8s.io/v1" "StorageClass" "" "").items }}
    {{- $params := $sc.parameters | default dict }}
    {{- if and (eq $sc.provisioner "linstor.csi.linbit.com") (ne (index $params "linstor.csi.linbit.com/allowRemoteVolumeAccess" | default "true") "false") }}
    {{- $_ := set $out $sc.metadata.name (dict "infraStorageClass" $sc.metadata.name "default" false) }}
    {{- end }}
    {{- end }}
    {{- /* Non-destructive fallback for dry-run/empty lookup: preserve the legacy mapping, never collapse to nothing. */}}
    {{- if not $out }}
    {{- $_ := set $out "replicated" (dict "infraStorageClass" "replicated" "default" false) }}
    {{- end }}
    {{- /* Mark exactly one default among the propagated classes. Chosen BEFORE the kubevirt alias is
           added below, so the legacy alias (which points at $defaultName, possibly absent on the infra
           cluster) can never be auto-selected as the tenant default: the configured storageClass if
           propagated, else replicated, else first by name. */}}
    {{- $chosen := "" }}
    {{- if hasKey $out $defaultName }}{{ $chosen = $defaultName }}
    {{- else if hasKey $out "replicated" }}{{ $chosen = "replicated" }}
    {{- else }}{{ $chosen = (keys $out | sortAlpha | first) }}{{ end }}
    {{- /* Legacy alias so existing tenant PVCs bound to the "kubevirt" StorageClass keep working. */}}
    {{- if not (hasKey $out "kubevirt") }}
    {{- $_ := set $out "kubevirt" (dict "infraStorageClass" $defaultName "default" false) }}
    {{- end }}
    {{- $_ := set (index $out $chosen) "default" true }}
    {{- $out | toYaml | nindent 6 }}
  dependsOn:
  - name: {{ .Release.Name }}-vsnap-crd
    namespace: {{ .Release.Namespace }}
  - name: {{ .Release.Name }}-cilium
    namespace: {{ .Release.Namespace }}
{{- end }}
