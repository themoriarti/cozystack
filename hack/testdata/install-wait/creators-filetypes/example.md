# Prose that shows the CR without creating one

The sweep's root holds documentation, and documentation quotes manifests:

```yaml
apiVersion: apps.cozystack.io/v1alpha1
kind: Kubernetes
metadata:
  name: "example"
```
